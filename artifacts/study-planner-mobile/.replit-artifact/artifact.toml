kind = "mobile"
previewPath = "/mobile/"
title = "Programme d'étude — Mobile"
version = "1.0.0"
id = "artifacts/study-planner-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 25859

[services.development]
run = "pnpm --filter @workspace/study-planner-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/study-planner-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/study-planner-mobile", "run", "serve" ]

[services.env]
PORT = "25859"
BASE_PATH = "/mobile/"
